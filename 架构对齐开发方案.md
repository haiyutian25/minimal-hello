# 架构对齐开发方案

> 依据：Android 官方《应用架构指南》(developer.android.com/topic/architecture)、
> 官方参考应用 Now in Android 的架构文档、官方 StateFlow/SharedFlow 指南，
> 并结合本项目 `架构开发方案.md` 的目标与当前 `minimal-hello` 代码现状制定。
>
> 文档目的：把当前项目逐步对齐到"目标架构"，给出差距分析、改造方案与实施步骤。

---

## 1. 目标架构（来自 `架构开发方案.md`）

- **架构模式**：MVVM、单向数据流（UDF）、Clean Architecture
- **异步与响应式数据流**：Kotlin Coroutines、Flow / StateFlow（一次性事件用 Channel，已在 M3 落地）
- **网络通信**：Retrofit、OkHttp、kotlinx.serialization
- **本地数据**：Room、DataStore

### 1.1 与官方口径的校准（重要）

官方（含 Now in Android）明确表示：推荐的是 **Android 官方分层架构**，而**不等同于严格的 Clean Architecture**。二者关键差异：

| 维度 | 严格 Clean Architecture | 官方推荐（本项目对齐目标） |
|---|---|---|
| 分层 | 强制 domain/use-case | UI + Data 必有，**domain 为可选层** |
| domain 层 | 所有逻辑过 use-case | **保持轻量**：仅用于"组合多 Repository / 转换模型 / 复用读取逻辑" |
| 事件类 use-case | 常见 | **官方不推荐**：用户操作事件由 UI 直接调用 Repository 的 suspend 方法 |

**结论**：本项目把 `架构开发方案.md` 里的 "Clean Architecture" 落地为**官方三层架构（UI / 可选 domain / Data）**，domain 层按需、轻量引入，避免过度设计。

---

## 2. 官方架构核心要点（调研结论）

1. **分层**：UI 层（Compose + ViewModel）→（可选）domain 层（UseCase）→ 数据层（Repository + DataSource）。
2. **单向数据流 UDF**：状态向下流（数据层→ViewModel→UI），事件向上流（UI→ViewModel→数据层）。
3. **单一可信来源 SSOT**：每类数据只有唯一所有者可修改；离线优先应用中 **本地数据库（Room）是 SSOT**。
4. **ViewModel**：管理界面状态、暴露 `StateFlow`、不持久化业务数据、生命周期与界面一致。
5. **Repository**：数据层唯一对外入口，协调多数据源、解决本地/远程冲突；**读取返回 `Flow`，写入用 `suspend` 函数**。
6. **DataSource**：每个数据源只负责一个来源（Room / 网络 / 文件）。
7. **离线优先 offline-first**：远程数据先写本地，UI 只读本地 Flow；网络失败不阻塞 UI；同步交给 `WorkManager`（失败指数退避）。
8. **UI 状态**：用**不可变 sealed interface/class** 建模（如 `Loading` / `Success`），ViewModel 用 `stateIn` 把冷 Flow 转热 `StateFlow`。
9. **事件流**：
   - `StateFlow`：持久状态（有当前值、可读 `value`）。
   - `SharedFlow`：一次性/广播事件（`replay`、`onBufferOverflow` 可配）。
   - `Channel`：点对点、保证只被消费一次的严格事件。
   - UI 收集用 `repeatOnLifecycle(STARTED)`，**勿用 `launch`/`launchIn` 直接收集**。
10. **依赖注入**：官方推荐 **Hilt**。
11. **序列化**：kotlinx.serialization 为 Kotlin 官方序列化方案，**编译期生成、无反射、空安全**，性能优于 Gson/反射方案。

---

## 3. 当前项目架构现状

### 3.1 模块结构
```
app                      // Activity、主题装配、DI 入口
core:ui                  // 主题（CssVariables/MinimalTheme）、共享 UI
core:data                // Repository、模型、DI（DataModule）
core:database            // Room（AppDatabase/Dao/迁移）
core:network             // Retrofit/OkHttp/Moshi 接线（GreetingApi、NetworkModule）
core:navigation          // 导航
feature:greeting:api     // feature 对外 API
feature:greeting:impl    // GreetingViewModel、screens、components
```

### 3.2 已符合项
- **MVVM**：`GreetingViewModel` 持有状态，屏幕经 `StateFlow` 观察。✅
- **UDF**：ViewModel 暴露 `StateFlow`，UI collect，事件经方法回传。✅
- **Coroutines / Flow / StateFlow**：`viewModelScope`、`Dispatchers.IO`、`stateIn`、`combine` 等大量使用。✅
- **Hilt DI**：`DataModule`/`NetworkModule`/`DatabaseModule` 均用 Hilt。✅
- **Room 持久化**：偏好数据走 Room + 迁移。✅

### 3.3 差距项（与目标/官方不符）
| # | 差距 | 现状 | 目标 |
|---|---|---|---|
| G1 | 序列化 | 网络层用 **Moshi**（`converter-moshi`+`moshi-kotlin`） | **kotlinx.serialization** |
| G2 | 网络层未激活 | `GreetingApi` 空接口、BASE_URL 占位 `api.example.com`、**无模块依赖 core:network**、`GreetingRepository` 仍本地静态 | 真实可用的离线优先数据层 |
| G3 | 事件流缺失 | 一次性事件（Toast 等）直接触发，未用 `SharedFlow`/`Channel` | 一次性事件走 `SharedFlow`/`Channel` |
| G4 | domain 层缺失 | ViewModel 直接依赖 Repository | 按需引入轻量 use-case（可选） |
| G5 | UI 状态建模 | 多为零散 `StateFlow` 字段 | 关键屏幕用 sealed UiState（`Loading/Success`） |

---

## 4. 对齐方案（分项）

### 方案 A：序列化切换到 kotlinx.serialization（对齐 G1）
**动机**：与 `架构开发方案.md` 一致；编译期生成、无反射、Kotlin 空安全、性能更优。

**改动**：
1. `gradle/libs.versions.toml`：
   - 增加 `kotlinx-serialization-json`、Retrofit 的 `converter-kotlinx-serialization`。
   - 引入 `org.jetbrains.kotlin.plugin.serialization`（KSP/编译器插件）。
2. `core:network/build.gradle.kts`：
   - 应用 `kotlinx-serialization` 插件。
   - 依赖改为 `converter-kotlinx-serialization` + `kotlinx-serialization-json`，移除 `converter-moshi`/`moshi-kotlin`。
3. `NetworkModule`：`MoshiConverterFactory` → `Json.asConverterFactory()`（配置 `ignoreUnknownKeys=true`、`isLenient` 等）。
4. 网络 DTO 用 `@Serializable` 标注。

**验收**：`core:network` 编译通过，Retrofit 使用 kotlinx-serialization 解析。

---

### 方案 B：激活网络层（对齐 G2）
**动机**：当前网络是"休眠脚手架"，未产生价值；对齐官方"Repository 为唯一数据入口"。

**就绪化（不依赖真实后端）**：
- 给 `GreetingApi` 定义示例端点契约（如拉取 `HeroQuote` 列表的 DTO）。
- 配置可注入的 `BASE_URL`（从 BuildConfig/配置注入，替代硬编码占位）。
- 让 `core:data` 依赖 `core:network`，`GreetingRepository` 具备"远程 + 本地兜底"的形态（远程不可用时回退本地静态/缓存）。

> 注：**离线优先（Room 作为 SSOT + WorkManager 同步）暂不需要，本期不纳入**；待有真实后端与离线诉求后再评估。

**验收**：`GreetingRepository` 可切换远程/本地，远程不可用时回退本地。

---

### 方案 C：一次性事件改用事件流（对齐 G3）
**动机**：对齐官方事件处理；避免把一次性事件当状态。

**选型**：
- 广播/可重放事件 → `SharedFlow(replay=…)`。
- 严格只消费一次（如导航、Toast）→ `Channel`（或 `SharedFlow(replay=0)` + `BufferOverflow.DROP_OLDEST`）。

**改动**：
- 在需要一次性事件的 ViewModel 增加 `private val _events = MutableSharedFlow<Event>(...)`（或 `Channel<Event>`），对外暴露只读流。
- UI 用 `repeatOnLifecycle(STARTED)` 收集（Compose 中用 `LaunchedEffect` + 生命周期感知收集）。
- 将现有直接触发的 Toast/导航等迁移为事件发射。

**验收**：一次性事件经事件流消费，配置变更后不重复触发。

---

### 方案 D：按需引入轻量 domain 层（对齐 G4，可选）
**动机**：当出现"多 ViewModel 复用同一逻辑 / 组合多 Repository / 转换模型"时再引入，避免过度设计。

**原则（官方）**：
- domain 层**可选、轻量**；UseCase 通常只有一个 `operator fun invoke()`。
- **不为事件建 use-case**；事件由 UI 直接调 Repository 的 suspend 方法。

**改动（当触发条件满足时）**：
- 新增 `core:domain`（或 feature 内 domain 包），放置如 `GetXxxUseCase`。
- ViewModel 改为依赖 UseCase 而非直接依赖多个 Repository。

**验收**：复用逻辑集中、ViewModel 更薄；无 UseCase 滥用。

---

### 方案 E：关键屏幕 UI 状态密封建模（对齐 G5）
**动机**：官方推荐用不可变 sealed UiState，编译期覆盖所有状态分支。

**改动**：
- 对"有加载/成功/失败"的屏幕（尤其接入网络后），定义 `sealed interface XxxUiState { Loading / Success(...) / Error(...) }`。
- ViewModel 用 `stateIn` 输出 `StateFlow<XxxUiState>`。

**验收**：UI 必须处理全部状态分支，无非法中间态。

---

## 5. 目标模块结构（对齐后）
```
app
core:ui
core:data          // Repository + 模型 + DI
core:database      // Room（本地持久化）
core:network       // Retrofit + OkHttp + kotlinx.serialization（被 core:data 依赖）
core:navigation
core:domain        // 【可选】轻量 UseCase（方案 D 触发时新增）
feature:greeting:api
feature:greeting:impl
```

依赖方向（单向）：
```
feature:impl → core:domain(可选) → core:data → core:network / core:database
feature:impl → core:ui / core:navigation
app → feature:impl / core:ui
```

---

## 6. 实施步骤 / 里程碑

| 阶段 | 内容 | 依赖 | 风险 | 状态 |
|---|---|---|---|---|
| M1 | 方案 A：序列化切到 kotlinx.serialization | 无 | 低（仅网络模块，当前未联网） | ✅ 已完成 |
| M2 | 方案 B1：网络层就绪化（端点契约 + BASE_URL 注入 + data 依赖 network + 本地兜底） | M1 | 低-中 | ✅ 已完成 |
| M3 | 方案 C：一次性事件改事件流 | 无（可与 M1/M2 并行） | 低 | ✅ 已完成 |
| M4 | 方案 E：关键屏幕 sealed UiState | 建议 M2 后 | 低 | ⏸ 暂缓：无真实后端，加载态空转；且本地引用用资源 ID（应用内切语言）、远端为纯文本，统一成本高。待接入真后端/真异步加载再落地，避免过度设计 |
| M6 | 方案 D：按需 domain 层 | 出现复用需求时 | 低（避免过度设计） | ⏸ 未触发：暂无多 ViewModel 复用/组合多 Repository 的需求 |

**建议顺序**：M1 → M2 → M3（并行）→ M4 →（按需）M6。

**落地结论（2026-09）**：M1/M2/M3 已完成并提交；M4、M6 按"勿过度设计"原则暂缓，待出现真实触发条件（真后端 / 复用需求）再启动。

---

## 7. 风险与注意事项

1. **勿过度设计**：domain 层可选、轻量；不为事件建 use-case（官方明确）。
2. **序列化切换影响面**：当前网络未真正使用，Moshi→kotlinx.serialization 风险低；若后续已有 Moshi DTO 需迁移 `@Serializable`。
3. **网络层依赖真实后端**：在无可用 API 前，以"就绪化 + 本地兜底"落地，避免空转。
4. **事件流收集生命周期**：务必 `repeatOnLifecycle(STARTED)`，避免 `launch`/`launchIn` 直接收集导致后台仍处理事件。
5. **渐进式**：每项独立可交付、可回滚，不做一次性大重构。

---

## 8. 与 `架构开发方案.md` 的映射结论

| 方案项 | 对齐动作 | 对应里程碑 |
|---|---|---|
| MVVM / UDF | 已符合，保持 | — |
| Clean Architecture | 落地为官方三层 + 可选轻量 domain | M6（按需） |
| Coroutines / Flow / StateFlow | 已符合，保持 | — |
| SharedFlow / Channel | 一次性事件改事件流 | M3 |
| Retrofit / OkHttp | 已接线，激活并注入 BASE_URL | M2 |
| kotlinx.serialization | Moshi → kotlinx.serialization | M1 |
